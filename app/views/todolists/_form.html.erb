<%= form_for @todolist, :html => { :class => "form-horizontal todolist" } do |f| %>

    <% if @todolist.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@todolist.errors.count, "error") %> prohibited this todolist from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @todolist.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="control-group">
    <%= f.label :listname, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :listname, :class => 'form-control' %>
    </div>
    <%= error_span(@todolist[:listname]) %>
  </div>


  <%= f.submit nil, :class => 'btn btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
            todolists_path, :class => 'btn btn-default' %>

<% end %>
